<Window x:Class="Mathenger.windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mathenger"
        xmlns:component="clr-namespace:Mathenger.components"
        mc:Ignorable="d"
        Name="mainWindow"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition/> 
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0" Grid.Row="0">
            <Label Content="{Binding Account.FirstName}"/>
            <Label Content="{Binding Account.LastName}"/>
        </StackPanel>
        <component:SideMenuComponent Grid.Row="1" Grid.Column="0"/>
        <component:ChatListComponent Chats="{Binding Chats, ElementName=mainWindow}" Grid.Row="1" Grid.Column="1" />
    </Grid>
</Window>
